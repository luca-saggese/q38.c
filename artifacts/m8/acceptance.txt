M8 acceptance status (full R1 artifact)

PASS
- Full R1 selective Q4 GGUF is preserved at 132212799456 bytes and recorded in
  R1_checksums.txt; M7/M2 artifacts were not deleted.
- Inventory validation, Q4 alignment, inspect, quant audit, and Q4 scalar/CUDA
  routed-expert parity pass.
- Real R1 CUDA generation runs pass without NaN/Inf; raw 1-cold+5-warm runs
  and their aggregate are recorded in r1_bench_runs/ and R1_memory_bench.json.
- M5-C12 is classified as an obsolete deleted-fixture gate. The direct full-R1
  replacement passes; see artifacts/m5/m5_c12_modern_gate.json.
- M1/M3 Makefile gates now bind the available full runtime artifact and do not
  recreate the deleted layers0-3 fixture.
- M9 is suspended; no M9 command was started.

CONDITIONAL / EXACT BLOCKERS
- tools/q38_eval.py now computes argmax, top-k, logit MAE/RMSE/max, KL,
  router-top-10 stability, and QSA selected-ID stability, but no paired finite
  BF16-reference/full-R1 records exist. The 32-sequence corpus is frozen and
  metrics remain not-computed; no metrics are fabricated.
- The same-process M7 benchmark could not be rebuilt because unrelated
  q38_forward CUDA files contain unresolved merge markers. The real R1 runs
  therefore record process-isolated timings; persistent non-PLE bytes,
  unified peak, MemAvailable, upload bytes/token, and residency misses are
  explicitly null in R1_memory_bench.json.
- Long-context startup/prefill instrumentation remains unavailable.
- M8 remains conditional, not PASS, until paired quality vectors and the
  required residency telemetry are available.
