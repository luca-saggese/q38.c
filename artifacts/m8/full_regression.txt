M8 regression run (real commands, 2026-09-04)

PASS
  make m0-acceptance
  make m1-validate m1-quant-block (inventory, manifest, donor block checks)
  make m2-acceptance
  direct M3 GR/GDN/state/chunk probes and test_weights against full R1 layers 0..3
  make m4-acceptance
  independent M6 binaries: test_m6_moe_binding, test_m6_moe_ref,
    test_m6_moe_cuda, test_m6_dispatch, test_m6_ubatch, test_m6_forward_api
  make m7-acceptance
  make m8-q4-kernel
  full R1 conversion, q38 --inspect, full R1 q38 --generate (1 token and 8 tokens)
  full R1 quant audit, manifest validation, and Q4 block alignment
  paired 4-record frozen-corpus greedy comparison: exact match, zero token changes

BLOCKED/FAILED
  make m1-acceptance/m3-audit could not be rebuilt in this worktree because
    unrelated q38_forward CUDA files contain unresolved merge markers; their
    recipes now use the available full runtime artifact and do not recreate
    the deleted layers0-3 fixture.
  M5-C12's old fixture coordinate mismatch (index 2560,
    -0.0039879242 vs -0.0011472413) is classified obsolete, not an actual
    regression; the direct full-R1 replacement passes in
    artifacts/m5/m5_c12_modern_gate.json.
  M6 chained acceptance was not rerun; independent M6 tests remain recorded.
  Full M0-M7 sweep remains opt-in in the Makefile (M7_RUN_FULL=1).

QUALITY/MEMORY LIMITS
  Full R1 now materialized: 132212799456 bytes; audit and runtime generation pass.
  q38_eval greedy comparison passes, but perplexity/NLL/KL/layer-logit metrics are
  not computed by the available evaluator; no stronger quality claim is made.
  A deterministic 32-sequence corpus and paired evaluator are implemented,
  but full BF16-reference/R1 vectors are unavailable, so argmax/top-k/logit/KL/
  router/QSA quality metrics remain not-computed.
  Long-context Spark startup/prefill peak instrumentation is unavailable.
  Six real process-isolated R1 runs are recorded in R1_memory_bench.json;
  persistent non-PLE residency, unified peak, MemAvailable, upload bytes/token,
  and residency misses remain explicitly unavailable.
